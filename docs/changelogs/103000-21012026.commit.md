Integrate Vibe Kanban Web Companion for in-iframe previews and click-to-source editing.

- Accomplished: Added `vibe-kanban-web-companion` dependency and enabled it for both the Phaser app (`/`) and Sprite Refiner (`/builder/`) during development.
- Accomplished: Verified the dev server emits JSX source metadata needed for click-to-component.
- Future work: Validate the workflow inside Vibe Kanban with both `npm run dev` and `npm run preview` if production-preview support is needed.
