Add Smart Refine button to auto-center sprites and clean backgrounds

- Added a top-bar `Smart Refine` action in the Sprite Refiner to process the currently selected sprite.
- Implemented background detection via dominant outside/border sampling + flood-fill masking, then fuzzy background-color cleanup inside the sprite bounds.
- Auto-computed a tight bounding box and centered it on the canvas origin by setting sprite offsets.
- Stored Smart Refine notes (detected background + bbox size) for review, plus a one-click revert of the last Smart Refine run (per-sprite).

Future work:
- Expose thresholds (flood/cleanup/alpha cutoff) in the UI for tricky sprite sheets.
- Add a per-sprite before/after toggle and a diff/overlay preview mode.
